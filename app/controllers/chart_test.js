'use strict';

describe('djoroDashboard.chart module', function() {

  beforeEach(module('djoroDashboard.chart'));

  describe('chart controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var chartCtrl = $controller('ChartCtrl');
      expect(chartCtrl).toBeDefined();
    }));

  });
});
